import { container, invite } from "./hero.css";

export default function Hero() {
  return (
    <header className={container}>
      <h1 className={invite}>루나 & 루미의 첫번째 생일을 축하해주세요</h1>
    </header>
  );
}
